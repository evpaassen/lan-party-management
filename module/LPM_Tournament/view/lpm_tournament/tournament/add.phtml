<h1>Create a tournament</h1>

<?php
$form = $this->form;
$form->setAttribute('action', $this->url('lpm_tournament/default', array('controller' => 'tournament', 'action' => 'add')));
$form->prepare();

echo $this->form($form);